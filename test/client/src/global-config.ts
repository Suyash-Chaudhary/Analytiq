class GlobalConfig {
  static readonly MAX_BUFFER_SIZE = 2000;
  static readonly EVENT_BUFFER_TIMEOUT = 2000;
  static readonly MAX_RECONNECT_ATTEMPTS = 10;
  static readonly MAX_RECONNECT_INTERVAL = 30000;
  static readonly RECONNECT_INTERVAL = 1000;
}

export default GlobalConfig;
